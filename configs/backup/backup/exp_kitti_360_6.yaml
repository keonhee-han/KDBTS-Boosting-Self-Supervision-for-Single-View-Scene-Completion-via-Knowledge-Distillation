defaults:                       ### This code is to run for slurm config
    - exp_kitti_360_base
    - _self_

name: "NeuRay"     ## Please name it in case there are various hparam settings to distinguish the diff results easily.

batch_size: 1
validate_every: 40000
visualize_every: 2000 

model_conf:
    num_layers: 1   
    NeuRay: True
    use_viewdirs: True   